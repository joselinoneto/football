// Copy this file to Secrets.swift (git-ignored) and fill in the values
// from cup_api/.env — the same credentials the app uses. The widget needs
// them so it can refresh scores on its own timeline. Or run
// ci_scripts/ci_post_clone.sh to generate it.
import FootballAPI

extension AirtableConfiguration {
    static let current = AirtableConfiguration(
        baseID: "appXXXXXXXXXXXXXX",
        token: "patXXXXXXXXXXXXXX"
    )
}
